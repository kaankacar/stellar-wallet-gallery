# Privy credentials — create an app at https://dashboard.privy.io
# App ID: Dashboard → your app → Settings (also shown during app creation).
VITE_PRIVY_APP_ID=

# Optional. Only needed if your Privy app requires a client ID
# (Dashboard → Settings → Clients). Leave empty otherwise.
VITE_PRIVY_CLIENT_ID=
