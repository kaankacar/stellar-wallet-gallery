# Blux app id — sign in at https://dashboard.blux.cc, create an app, copy its id.
# Blux validates the appId against its API; login/signing refuse to run with an
# invalid one, so a real dashboard app id is required.
VITE_BLUX_APP_ID=
